name: Build and release Eyelash Sofle firmware

on:
  workflow_dispatch:
  push:
    paths:
      - 'boards/arm/eyelash_sofle/**'
      - 'config/eyelash_sofle.*'
      - 'build_eyelash_sofle.yaml'
      - 'config/west.yml'
      - 'zephyr/**'
permissions:
  contents: write

jobs:
  draw:
    name: Draw Eyelash Sofle Keymap
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
    with:
      destination: artifact
      artifact_name: eyelash-sofle-keymap
      fail_on_error: true
      keymap_patterns: 'config/eyelash_sofle.keymap'
  build:
    name: Build Eyelash Sofle
    needs: draw
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: build_eyelash_sofle.yaml
      archive_name: eyelash-sofle-firmware
  release:
    name: Release Eyelash Sofle
    needs: [build, draw]
    if: success() && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: eyelash-sofle-firmware
          path: ./artifacts
      - name: Download keymap artifact
        uses: actions/download-artifact@v4
        with:
          name: eyelash-sofle-keymap
          path: ./keymap_artifacts
      - name: Convert keymap SVG to PNG
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends librsvg2-bin
          for f in ./keymap_artifacts/*.svg; do
            [ -f "$f" ] || continue
            png="${f%.svg}.png"
            rsvg-convert "$f" -o "$png"
            echo "Converted $f -> $png"
          done
      - name: Generate build info
        id: info
        run: |
          echo "timestamp=$(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_OUTPUT
          echo "short_sha=${GITHUB_SHA:0:7}" >> $GITHUB_OUTPUT
      - name: Delete existing tag
        uses: dev-drprasad/delete-tag-and-release@v1.0
        with:
          tag_name: latest-eyelash_sofle
          github_token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: latest-eyelash_sofle
          name: Eyelash Sofle Latest Firmware
          body: |
            🔁 **Eyelash Sofle Build**
            Commit: ${{ steps.info.outputs.short_sha }}
            Built: ${{ steps.info.outputs.timestamp }}
            Install: flash left/right .uf2 files.
            Keymap PNG: Download from release assets
          files: |
            ./artifacts/**/*.uf2
            ./keymap_artifacts/**/*.png
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
